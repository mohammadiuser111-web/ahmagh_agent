# 🚀 گزارش فاز ۵ — محیط تست ایزوله + یادآوری داینامیک + منو + ابزار ادمین

**تاریخ:** ۳۱ شهریور ۱۴۰۵ · **کامیت:** `e86a518` (push شده) · **نسخه‌ی دیپلوی:** `5053c57e`
**تست‌ها:** ✅ **۱۱۴ از ۱۱۴ سبز** (۶۵ قدیمی + ۴۹ جدید) · `tsc` بدون خطا

---

## ۱) 🧪 محیط تست ایزوله (خواسته‌ی اول)

بات واقعی دست‌نخورده ماند؛ همه‌چیز پشت صحنه تست شد:

| قطعه | فایل | نقش |
|---|---|---|
| دیتابیس فیک | `test-env/d1.ts` | D1 شبیه‌سازی‌شده با better-sqlite3 (همان API ورکر + اجرای خودکار migrationها) |
| هارنس | `test-env/harness.ts` | تلگرامِ ماک (همه‌ی پیام‌ها/اسناد در outbox)، هوش مصنوعیِ فیک با صف پاسخ، ساعت مجازی |
| صحنه | `createScene()` | سه کاربر: **اصغر** (ادمین `@asghar_admin`)، **علی** (`@ali_user`)، **ممد** (`@mmd_user`) |

- **تاریخ و ساعت واقعی شمسی** (همان `todayTehranISO` کد اصلی)، شروع صحنه ۱۰:۰۰ صبح تهران.
- **زمان با سرعت دلخواه جلو می‌رود** (`advance(ms, speed)`): هر گام، کرونِ واقعیِ `worker.scheduled` را روی مرزهای ۱۰دقیقه‌ای مجازی اجرا می‌کند — یعنی دقیقاً همان موتوری که روی کلادفلر می‌چرخد.
- تست ماراتن: **۷ روز مجازی با سرعت ۷x** (۷ تسک همزمان، ~۱۰۰۰ اجرای کرون) — بدون هیچ خطا، با شمارش دقیق پیام‌ها.

## ۲) 🔔 یادآوری داینامیک (خواسته‌ی دوم — نه ساعتِ ثابت خودکار!)

الگو را **خود کاربر موقع ساخت مشخص می‌کند** و بات از متن فارسی می‌فهمد:

| می‌گویی | می‌شود |
|---|---|
| «هر روز ساعت ۵ یاداوری کن» | روزانه ۰۵:۰۰ — دقیقاً یک‌بار در روز، همان روز اول اگر ساعت نرسیده باشد |
| «هرموقع ۱ ساعت به ددلاین مونده بود پیام بده» | فقط یک‌بار در پنجره‌ی (ددلاین−۱ساعت تا ددلاین+۶ساعت) |
| «هر ۳ ساعت یادم کن» | اولین پیام یک بازه‌ی کامل بعد از ساخت، بعد هر ۳ ساعت |
| «فردا ساعت ۹ صبح یادم بنداز» | یک‌بار در همان لحظه |
| «یادآوری نکن» | خاموش مطلق |
| *(هیچی نگفتی)* | همان الگوریتم پلکانی قبلی (پیش‌فرض/فالبک) |

- کارت تسک خطِ «🔔 یادآوری: …» را با تاریخ شمسی نشان می‌دهد.
- `/edit <id> یادآوری: هر ۲ ساعت` — تغییر الگو، صفرشدن شمارنده‌ها.
- تموم‌کردن تسک = قطع همه‌ی یادآوری‌ها. یادآوری همیشه به **مسئول** می‌رود.

## ۳) 👑 ابزار ادمین (خواسته‌ی چهارم)

- «احمق برای @ناشناس یه تسک بساز…» → کارت + **لیست انتخاب کاربر** (دکمه‌های شیشه‌ای) → با انتخاب، تسک واگذاری و به کاربر خبر می‌رسد.
- «احمق برای علی یه تسک بساز…» و «برای @ali_user» → مستقیم مسئول درست + خبر به او با نام ادمین.
- **«تسک‌های علی چیا هستن؟»** → لیست + «⏰ نزدیک‌ترین ددلاین» + تعداد تمام‌شده‌ها. (کاربر عادی: رد با «فقط ادمین»)
- کاربر عادی نمی‌تواند برای دیگری تسک بسازد (خودش مسئول می‌شود + پیام).
- «🌐 تسک‌های همه» → همه‌ی تسک‌های باز همه‌ی کاربرها، مرتب‌شده با نزدیک‌ترین ددلاین.

## ۴) 🎛 منو و خروجیِ تمیز (خواسته‌ی پنجم)

- **کیبورد دائمی پایین چت**: ➕ تسک جدید · 📋 تسک‌های من · 🕘 تموم‌شده‌ها · 📤 خروجی · ❓ راهنما — و فقط برای ادمین: 👥 کاربرها · 🌐 تسک‌های همه (برای کاربر عادی این دو دکمه وجود ندارد و دسترسی‌اش هم قطع است).
- **باگ خودت گفتی درست شد:** «خروجی تسک های منو بده» حالا مستقیم به انتخابگر فرمت (HTML/PDF) می‌رود — نه «تسک بسازم؟» نه لیست. «تاریخچه/گزارش تسک‌هام» هم همین‌طور.
- خروجی HTML و PDF (فونت فارسی) هر دو در محیط ایزوله واقعاً تولید و ارسال شدند.

## ۵) 🧠 پرامپت قوی‌تر (خواسته‌ی سوم)

پرامپت سیستم بازنویسی شد: قواعد دقیق هر فیلد + الگوهای یادآوری + **دو مثال few-shot با تاریخ واقعی روز** + لیست کاربرهای شناخته‌شده. تستِ مسیر AI (جیسون مدل → مسئول و یادآوری) هم اضافه شد.

---

## 🐛 حفره‌هایی که تست‌ها پیدا کردند و همان‌جا فیکس شدند

1. **باگ اصلی شما:** «خروجی تسک های منو بده» قبل از خروجی به تشخیصِ «لیست» می‌رسید → ترتیب چک عوض شد + گارد داخل تشخیص لیست.
2. **دکمه‌های منو با نیم‌فاصله اصلاً کار نمی‌کردند** («📋 تسک‌های من»، «🕘 تموم‌شده‌ها»، «🌐 تسک‌های همه») → نرمال‌سازی دوطرفه.
3. **دو کوئری D1 بدون `bind()`** (لیست انتخابگر ادمین و تسک‌های کاربر) → کرش. 
4. `fmtDateTimeFa` تاریخ را **میلادی** چاپ می‌کرد → شمسی شد («۱ مهر ۱۴۰۵ — ساعت ۱۰:۰۰»).
5. عبارت خود شما «**۱ ساعت به ددلاین مونده بود**» پارس نمی‌شد → regex واقعی‌تر شد.
6. `/edit … یادآوری: نکن` کار نمی‌کرد.
7. تسکِ با شروعِ امروز «شروع‌نشده» می‌ماند → حالا موقع ساخت «در حال انجام» است (انتقال خودکار، طبق خواسته‌ی قبلی).
8. یادآوری روزانه بعد از ساخت/ویرایشِ تاریخ، تا فردا بلاک می‌شد (`last_reminded_at`) → برای الگوهای داینامیک قواعد خودشان حاکم است.

## 📦 تست‌ها (مجموع ۱۱۴)

| فایل | تعداد | چیزی که می‌پوشاند |
|---|---|---|
| `test-env/reminders.env.test.ts` | ۱۰ | هر ۵ الگو + رگرسیون پلکانی + done-stops + شروع خودکار + ۴۸h با سرعت ۳x + کارت شمسی |
| `test-env/admin.env.test.ts` | ۹ | ساخت برای دیگران، @ناشناس→picker، کوئری زبانی، منو/دسترسی‌ها، تسک‌های همه |
| `test-env/flows.env.test.ts` | ۱۲ | باگ خروجی، HTML/PDF واقعی، لیست‌های زبانی، جریان pending با یادآوری، /edit، تمیزی متن، مسیر AI |
| `test-env/marathon.env.test.ts` | ۱ | ۷ روز × ۷ سرعت، ۷ تسک همزمان، شمارش دقیق (روزشانه=۷، هر۵ساعت=۳۳، …) |
| `test-env/reminder-spec.test.ts` | ۱۷ | واحدِ پارسر الگو و متن فارسی‌اش |
| تست‌های قبلی | ۶۵ | همه‌ی فازهای ۱–۴ |

## 🌍 دیپلوی (فاز ۵ روی لایو)

- مایگریشن `0005_dynamic_reminders` روی D1 ایمی **اعمال شد** ✅ (تسک‌های قدیمی `default` ماندند)
- ورکر دیپلوی شد: `https://ahmagh-agent.nova-0e7442.workers.dev` · نسخه `5053c57e` · کرون `*/10` · وبهوک سالم، صف خالی
- سلامت‌سنجی: `/health` ✅، `getMe` ✅، ستون‌های جدید روی دیتای واقعی ✅

## ⚠️ امنیت — لازم

توکن‌ها (گیت‌هاب، کلادفلر، بات، رمز ادمین) قبلاً در گفتگو لو رفته‌اند. لطفاً **همه را چرخش (revoke/rotate) بده** و به‌جایشان از Secretهای کلادفلر و GitHub Actions استفاده کن — بات بعد از چرخش فقط نیاز دارد توکن جدید بات در `wrangler secret put TELEGRAM_BOT_TOKEN` ست شود.

---

**گام بعدی پیشنهادی:** از بات لایو همین امشب یک تست واقعی بگیر: «احمق یه تسک بساز: تست فاز ۵، تا فردا ساعت ۲۳ — هر روز ساعت ۲۲ یادم کن» و دکمه‌های منو را بزن. اگر خواستی، دور بعد می‌توانم تست‌های لایوِ کامل (مثل `live_test4.py`) را برای فاز ۵ هم بنویسم.
